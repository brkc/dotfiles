files=$(find . -name '.*' -not -name . \
               -not -name .git -not -name .mozilla)
for file in $files; do
  file=$(basename $file)
  if [[ -e ~/$file ]]; then
    echo skipping $file
    continue
  fi
  ln -s $PWD/$file ~/$file
done
